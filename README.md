# homebrew-hivemux

Homebrew tap for [hivemux](https://github.com/Bradical247/hivemux).

```bash
brew install bradical247/hivemux/hivemux
```

Installs the prebuilt binary for your platform (macOS arm64 / Linux x64). The web
GUI also needs `ttyd` (`brew install ttyd`) and a browser.
